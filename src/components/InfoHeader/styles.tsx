import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 40,
    backgroundColor: "#000",
  },
  topBar: {
    flexDirection: "row",
    height: 40,
    alignItems: "center",
  },
});

export default styles;
