import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },
  infoContainer: { padding: 50 },
  title: { fontSize: 32, fontWeight: "bold" },
  info: { fontSize: 22, marginTop: 20 },
});

export default styles;
